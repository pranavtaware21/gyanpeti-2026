node_modules
dist
.DS_Store
*.local
tsconfig.tsbuildinfo

# Generated by `npm run assets` from img-source/. Rebuilt in CI, so keeping
# it out avoids ~9MB of binaries in history that change on every re-encode.
public/img/derived

# Retired modules from the earlier fifteen-scene composition.
_attic

# Raw WhatsApp dumps the originals in img-source/ were renamed from.
_originals

# Design canvas snapshots and the pull diff.
_design
